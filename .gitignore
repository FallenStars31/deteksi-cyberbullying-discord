# Lingkungan Python & cache
__pycache__/
*.pyc
*.pyo
.venv/
venv/
env/

# Tooling internal
.claude/

# ─────────────────────────────────────────────────────────────
# PRIVASI: JANGAN publikasikan data pesan Discord asli
#   - datachatdiscord/: CSV mentah berisi username & isi pesan ASLI
#   - hasil/*.csv      : data_bersih & dataset berlabel (isi pesan)
# Aplikasi TIDAK butuh berkas ini saat berjalan (cukup model .joblib).
# ─────────────────────────────────────────────────────────────
datachatdiscord/*.csv
hasil/*.csv
# Database lokal SQLite: BERISI pesan asli -> jangan dipublikasikan
hasil/*.db

# Secrets (kredensial database Supabase) -> JANGAN commit
.streamlit/secrets.toml
# Database berisi isi pesan asli -> JANGAN dipublikasikan (dibuat ulang dari data)
hasil/*.db
# Kredensial database (Supabase dll.) -> RAHASIA
.streamlit/secrets.toml

# Naskah skripsi & catatan/panduan internal (merujuk naskah privat)
*.docx
konteks.md
Laporan_Perbaikan_Sistem.md
Revisi_Naskah_Skripsi.md
# Catatan pribadi (dev log & revisi naskah) — tidak dipublikasikan
log.md
revisi.md
# Screenshot antarmuka untuk naskah (bisa memuat isi pesan) — privat
screenshot/

# Berkas OS
.DS_Store
Thumbs.db
